apiVersion: v1
kind: Service
metadata:
  name: digitalazizi
  labels:
    app.kubernetes.io/name: digitalazizi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: digitalazizi
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
